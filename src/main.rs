use supervisor_core::StateMachine;

fn main() {
    println!("Testing Rust State Machine natively...");
    
    // In a real cargo binary context, we call the same structural library logic
    // Note: For this to compile cleanly as both a binary and a library,
    // you would use the internal logic or separate your code into modules.
    
    println!("State machine initialized successfully.");
}
